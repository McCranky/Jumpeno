@if (Show) {
    <div class="alert alert-warning" @onclick="OnClick">
        @Message
    </div>
}

@code {
    [Parameter]
    public string Message { get; set; }
    [Parameter]
    public bool Show { get; set; }
    [Parameter]
    public System.Action OnClick { get; set; }
}
